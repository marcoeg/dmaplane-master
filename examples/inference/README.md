# Inference Example

The disaggregated inference servers (`prefill_server.py` and `decode_server.py`) live in [`examples/kvcache/`](../kvcache/) alongside the C WRITEIMM tools and Python wrapper they depend on. See [`examples/kvcache/README.md`](../kvcache/README.md) for usage, CLI reference, and acceptance tests.
